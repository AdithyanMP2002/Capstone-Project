# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# Notion MCP Configuration
# If using stdio, these might be passed directly to the command in tools.py, 
# but good to have placeholders if using SSE or remote.
NOTION_API_KEY=secret_...

# Google Calendar MCP Configuration
# Typically handles auth via OAuth flow on startup, but placeholder for any needed config.
